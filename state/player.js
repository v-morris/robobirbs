const initialState = {
  hitpoints: 10,
};

function playerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      break;
  }

  return state;
}

module.exports = playerReducer;